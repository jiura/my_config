Switch "netrw.vim" in folder "...\nvim\share\nvim\runtime\autoload" for the one here.
That's a fix for netrw preview winsize not working properly when vertically split. (although I don't really use it anymore)
